const React = require('react')
const Def = require('./default')

function assign (data) {
    let customersFormatted = data.customers.map((places) => {
        return (
            <div>
                <main>
                    <h1>ASSIGN</h1>
                </main>
            </Def>
        )
    })
}

module.exports = assign